\section{Method}

Given a support set $S$, we encode it into a latent vector $z = f(S)$.

A hypernetwork maps $z$ to weights $w = g(z)$ of a target model.

A diffusion model is trained over latent codes to model a distribution over plausible solver representations.
