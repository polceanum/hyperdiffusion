\section{Introduction}

We study how to generate task-specific neural network weights from small support sets. Hypernetworks can map task representations to weights, but are typically deterministic. Many tasks admit multiple plausible solutions, especially under limited data.

We explore whether diffusion models can represent a distribution over solver latents conditioned on a support set.
